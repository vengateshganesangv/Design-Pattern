import { Address } from "../data/Address"; // Assuming the Address class is in a data directory. Adjust the path if necessary.

export class DeliveryManager {
  isDeliveryPossible(
    restaurantAddress: Address,
    customerAddress: Address
  ): boolean {
    return true;
  }
}
