import { Restaurant } from "../data/Restaurant";

export class GetRestaurantByIdAPI {
  getRestaurantById(id: number): Restaurant | null {
    // Possibly replace with the actual logic to get the restaurant by id
    return null;
  }
}
